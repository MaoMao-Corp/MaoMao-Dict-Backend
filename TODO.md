# TODO 
no funciona bien para cuando hay \' de por medio porque no se poner bien el indice xd
can be done more optimaly if we avoid passing a tuple with p and p.text_cleaned.
still **no** lemmatization done for the dictionary
